services:
  - type: web
    name: haynes282-digitaltwin
    env: python
    plan: free
    pythonVersion: 3.11.9
    buildCommand: python3.11 -m pip install --break-system-packages -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0